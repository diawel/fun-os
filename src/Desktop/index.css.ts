import { style } from '@vanilla-extract/css'

export const desktop = style({})

export const folderLabelColor = style({
  color: 'white',
})

export const folderPosition = style({
  display: 'flex',
  justifyContent: 'flex-end',
  padding: '40px 80px 0 0',
})
