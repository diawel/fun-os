import { style } from '@vanilla-extract/css'

export const cell = style({
  height: '82px',
})

export const roudedLeftCorners = style({
  borderRadius: '10px 0 0 10px',
})

export const roudedRightCorners = style({
  borderRadius: '0 10px 10px 0',
})
